# Cal.com event link in the form "user-slug/event-slug"
# Defaults to "outpost/15min" if unset.
NEXT_PUBLIC_CAL_LINK=outpost/15min

# Site origin used for metadataBase and absolute OG image URLs.
# Defaults to https://outpost.com.au if unset.
NEXT_PUBLIC_SITE_URL=https://outpost.com.au
